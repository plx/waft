---
title: "Configuration"
sidebar:
  label: "Configuration"
description: "Layered configuration and per-knob overrides."
---

`waft` resolves profile and policy settings from multiple layers. Later layers
win for scalar values.

1. Built-in defaults.
2. User config: `~/.config/waft/config.toml`.
3. Project configs: each `.waft.toml` in the resolved source worktree, from
   its repo root down to the invocation directory's equivalent source path.
4. Environment variables using the `WAFT_*` prefix.
5. CLI flags.

Project configs must be regular files. Symlinked configs fail closed, and
discovery stops at nested repositories and registered submodules.

Selecting a profile resets all profile-owned knobs at that layer. Explicit
knobs in that layer, or in a later layer, can override it; lower-layer knobs
cannot silently alter a higher-precedence profile.

For managed or reproducible operation, pass `--isolated`. It omits user
configuration, `WAFT_CONFIG_PATH`, and `WAFT_*` policy environment variables;
built-in defaults, source-project configuration, and CLI overrides still
apply. Operational selection of the Git backend remains available.

## Example `.waft.toml`

```toml
version = 1

[compat]
profile = "git"

[exclude]
extra = ["*.bak"]
```

## Per-knob CLI flags

| Option                                                       | Description                                       |
| ------------------------------------------------------------ | ------------------------------------------------- |
| `--compat-profile <claude\|git\|wt>`                         | Coordinated preset selection.                     |
| `--when-missing-worktreeinclude <blank\|all-ignored>`        | Behavior when no `.worktreeinclude` exists.       |
| `--worktreeinclude-semantics <claude-2026-04\|git\|wt-0.39>` | Matcher semantics version.                        |
| `--worktreeinclude-symlink-policy <follow\|ignore\|error>`   | How to handle symlinked rule files.               |
| `--builtin-exclude-set <none\|tooling-v1>`                   | Curated tool-state exclusion set.                 |
| `--extra-exclude <GLOB>`                                     | Repeatable additional excludes.                   |
| `--replace-extra-excludes`                                   | Drop inherited `extra-exclude` values.            |
| `--config <PATH>`                                            | Use this file instead of the default user config. |
| `--isolated`                                                 | Omit user and environment policy layers.          |
