# Docs Composites Batch Review

## Source Artifact

- Rejected CSS-contract poster: `.maquette/components/component-sheet-docs-composites-css-contract-v1.png`
- Accepted CSS-contract poster: `.maquette/components/component-sheet-docs-composites-css-contract-v2.png`
- Transcribed contract: `.maquette/components/contracts/docs-composites.contract.css`
- Batch replica: `.maquette/components/docs-composites.replica.html`
- Batch CSS: `.maquette/components/css/docs-composites.components.css`
- Batch JS: `.maquette/components/js/docs-composites.components.js`
- Catalog snapshot: `.maquette/components/docs-composites.component-catalog.json`

## Review Mode

Manual review. Automated browser screenshots, responsive overflow measurements,
and schema validation are blocked because project-local `playwright`, `ajv`, and
`ajv-formats` are unavailable, and this session cannot install network
dependencies.

## Rejection And Regeneration

The v1 poster was rejected because it was generated by deterministic raster
rendering rather than the required image-generation path and included `@media`
blocks outside the strict selector contract. The v2 poster was generated through
the image workflow, inspected, and accepted.

## Fidelity Rubric

- Coverage: 5. Command block, doc cards, equal-height card grid, and step-list
  selectors are implemented.
- Visual match: 4. CSS follows the poster contract and approved brand tokens;
  no screenshot comparison was available.
- Anatomy match: 5. Command header/body/copy, card eyebrow/title/body/footer,
  pinned footers, and step marker/content anatomy are preserved.
- Responsive match: 4. Grid and command lines are designed to avoid page-level
  overflow. Measured overflow QA is deferred.
- Implementation quality: 4. Semantic HTML, token usage, copy controls, and
  ordered-list step semantics are present.

## Corrections Made

- Rejected v1 and regenerated a compliant v2 poster.
- Mapped sparse poster values to approved token variables.
- Used `margin-top: auto` to keep repeated card action rows aligned.

## Status

Implemented before assembling the final component gallery.
